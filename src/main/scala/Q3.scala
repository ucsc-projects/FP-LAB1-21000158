import scala.math.Pi

object Q3 {
  def main(args: Array[String]) {
    var radius = 5;
    var volume = (4/3)*Pi * radius * radius*radius;
    println("Area of a radius 5 sphere : " + volume);
  }
}
